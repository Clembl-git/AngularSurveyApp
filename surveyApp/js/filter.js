//
// angular.filter('capitalize', function() {
//     return function(input) {
//       return (!!input) ? firstToUpperCase(input) : '';
//     }
// })
// angular.filter('firstLetterOnly', function() {
//     return function(input) {
//       return (!!input) ? input.charAt(0)+"." : '';
//     }
// })
// angular.filter('libelleDay', function() {
//     return function(input) {
//       return (!!input) ? moment(input).format('dddd') : '';
//     }
// })
// angular.filter('dayAndMonth', function() {
//     return function(input) {
//       return (!!input) ? moment(input).format('D, MMM') : '';
//     }
// })
// angular.filter('dateOnly', function() {
//     return function(input) {
//       return (!!input) ? moment(input).format('DD/MM/YYYY') : '';
//     }
// });
